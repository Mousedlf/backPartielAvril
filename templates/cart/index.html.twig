{% extends 'base.html.twig' %}

{% block title %}QUACK | Hello CartController!{% endblock %}

{% block body %}
    <div class="container">

        <h1>Cart</h1>

            <table class="table table-hover mb-4">
                <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Price</th>
                    <th scope="col">QR code</th>

                </tr>
                </thead>
                <tbody>
                {% for item in cart %}
                    <tr class="">
                        <td>{{ item.product.name }}</td>
                        <td>{{ item.product.price }} €</td>
                        <td>beau Qrcode</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>


    </div>


{% endblock %}